
example:
  jwt-secret: "hello"



spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oauth_db
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update    # ??? only
    show-sql: true


server:
  port: 9000


logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG